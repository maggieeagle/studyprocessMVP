<UserControl x:Class="UI.Views.StudentCoursesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:UI.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <helpers:BindingProxy x:Key="VmProxy" Data="{Binding}" />
    </UserControl.Resources>

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="📖 My Courses" 
                       Style="{StaticResource HeadingStyle}"
                       FontSize="26"
                       Margin="0,0,0,6"/>
            <TextBlock Text="Browse and enroll in courses to start your learning journey"
                       Style="{StaticResource SubheadingStyle}"
                       FontSize="14"/>
        </StackPanel>

        <!-- filters -->
        <Border Grid.Row="1"
                Style="{StaticResource CardStyle}"
                Padding="20"
                Margin="0,0,0,20"
                Visibility="{Binding Data.IsStudent, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <WrapPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock Text="Search" Style="{StaticResource LabelStyle}" FontSize="11"/>
                        <TextBox Width="180"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource BaseTextBoxStyle}"
                                 Padding="10,8"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,16,8">
                        <TextBlock Text="Course Code" Style="{StaticResource LabelStyle}" FontSize="11"/>
                        <TextBox Width="120"
                                 Text="{Binding CourseCode, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource BaseTextBoxStyle}"
                                 Padding="10,8"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,16,8">
                        <TextBlock Text="Status" Style="{StaticResource LabelStyle}" FontSize="11"/>
                        <ComboBox Width="140"
                                  ItemsSource="{Binding CourseStatusValues}"
                                  SelectedItem="{Binding CourseStatus, Mode=TwoWay}"
                                  Style="{StaticResource BaseComboBoxStyle}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FallbackValue=All}" Foreground="{StaticResource TextPrimaryBrush}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,0,16,8">
                        <TextBlock Text="Start Date" Style="{StaticResource LabelStyle}" FontSize="11"/>
                        <DatePicker SelectedDate="{Binding StartDate}" 
                                    Width="130"
                                    Style="{StaticResource BaseDatePickerStyle}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,16,8">
                        <TextBlock Text="End Date" Style="{StaticResource LabelStyle}" FontSize="11"/>
                        <DatePicker SelectedDate="{Binding EndDate}" 
                                    Width="130"
                                    Style="{StaticResource BaseDatePickerStyle}"/>
                    </StackPanel>
                </WrapPanel>

                <Button Grid.Column="1"
                        Content="🔍 Search"
                        Command="{Binding LoadCoursesCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Padding="20,10"
                        VerticalAlignment="Bottom"
                        Margin="0,0,0,8"/>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12">
            <DataGrid ItemsSource="{Binding Courses}"
                      Style="{StaticResource DataGridStyle}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      CellStyle="{StaticResource DataGridCellStyle}"
                      RowStyle="{StaticResource DataGridRowStyle}"
                      BorderThickness="0"
                      AlternationCount="2">

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Course Name" Width="*" MinWidth="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock VerticalAlignment="Center">
                                    <Hyperlink Command="{Binding DataContext.NavigateToCourseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}"
                                               Foreground="{StaticResource PrimaryLightBrush}"
                                               TextDecorations="None">
                                        <TextBlock Text="{Binding CourseName}" FontWeight="Medium"/>
                                    </Hyperlink>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Code"
                                        Binding="{Binding Code}"
                                        Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Start Date"
                                        Binding="{Binding StartDate, StringFormat='MMM dd, yyyy'}"
                                        Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="End Date"
                                        Binding="{Binding EndDate, StringFormat='MMM dd, yyyy'}"
                                        Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Status"
                                            Width="100"
                                            Visibility="{Binding Data.IsStudent, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12"
                                        Padding="10,4"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#1A64748B"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEnrolled}" Value="True">
                                                    <Setter Property="Background" Value="#1A22C55E"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="11" FontWeight="Medium" HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Available"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEnrolled}" Value="True">
                                                        <Setter Property="Text" Value="Enrolled"/>
                                                        <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header=""
                                            Width="Auto"
                                            Visibility="{Binding Data.IsStudent, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVis}}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.EnrollCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Padding="16,8"
                                        Margin="4,0">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource SuccessButtonStyle}">
                                            <Setter Property="Content" Value="Enroll"/>
                                            <Setter Property="IsEnabled" Value="True"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEnrolled}" Value="True">
                                                    <Setter Property="Content" Value="✓ Enrolled"/>
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
